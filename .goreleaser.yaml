version: 2
project_name: programmapper-pp-cli
changelog:
  disable: true
builds:
  - id: programmapper-pp-cli
    main: ./cmd/programmapper-pp-cli
    binary: programmapper-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X programmapper-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: programmapper-pp-mcp
    main: ./cmd/programmapper-pp-mcp
    binary: programmapper-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: programmapper-pp-cli
    repository:
      owner: johnnyrobot
      name: homebrew-tap
    homepage: "https://github.com/johnnyrobot/programmapper-pp-cli"
    description: "Every California Community College's program maps - degrees, courses, transfer paths, and careers - in one scriptable CLI with an offline SQLite catalog and term-by-term planning no other ProgramMapper tool has."
    install: |
      bin.install "programmapper-pp-cli"
      bin.install "programmapper-pp-mcp"
